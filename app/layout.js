export const metadata = {
  title: 'Bimbel Pro Enterprise - Upgrade Besar',
  description: 'Aplikasi bimbel premium multi cabang dengan payroll, laporan keuangan, dan barcode otomatis.',
}

import './globals.css'

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  )
}
